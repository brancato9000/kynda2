# Curated Overrides

Place JSON files here named by normalized subject (e.g. radiohead.json, bjork.json). These override AI-generated results. Format matches the mix API response structure with items array, each item having slotType, title, creator, year, reason, confidence, and sources. Sources can be strings or objects with label and url fields.
